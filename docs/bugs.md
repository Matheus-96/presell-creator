Bugs encontrados:

Ao iniciar uma analise com  IA tem algo errado com o polling. Quando clico em analisar ele dispara a request ao backend mas nao sou direcionado para o proximo passo. Isso está permitindo que eu gere diversos jobs no backend. Isso deve ser bloqueado direto no backend para evitar custos desnecessários de IA.

Algumas telas ainda estão sem CSS

Ao criar a presell como draft não está adicionando as imagens que selecionei, e o link de afiliado é um parâmetro obrigatório, pode criar ele com algum placeholder neste momento.