{
  "name": "presell-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Relocated Express backend for the presell creator split.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "lint": "eslint src --ext .js",
    "build": "node -e \"console.log('The relocated backend has no build step yet.')\""
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "express-session": "^1.19.0",
    "multer": "^2.1.1",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
